# OrangeHRM_AutomationProject

## 🚀 Project Overview  
This is an automation framework for testing the OrangeHRM web application. It uses Selenium WebDriver + Java (with Maven) to perform automated UI tests, making it easy to validate core HR workflows (login, employee management, admin tasks, etc.) with minimal manual effort.

## ✨ Features  
- End-to-end UI automation for OrangeHRM workflows — login, employee CRUD, admin functions.  
- Selenium WebDriver for browser automation.  
- Maven as build and dependency management tool.  
- Easily extendable: add new test scripts or modules without major rework.  
- Cross-browser support: ensure compatibility across different browsers.  

## 🛠️ Tech Stack  
- Java 17
- Selenium WebDriver  
- Maven (via `pom.xml`)  
- TestNG  

